<% include ../partials/header %>

<div class="ui main container">

<form class="ui form segment" action="/miti/<%= product._id %>?_method=PUT" method="POST">
    <div class="image">
      <img class="ui centered medium image" src="<%= product.image %>" alt="">
    </div>
    <div class="field">
      <label>Link da imagem</label>
      <input type="text" name="product[image]" value="<%= product.image %>">
    </div>
    <div class="field">
      <label>Nome do produto</label>
      <input type="text" name="product[name]" value="<%= product.name %>">
    </div>
    <div class="field">
      <label>Preço do produto</label>
      <input type="text" name="product[price]" value="<%= product.price %>">
    </div>
    <div class="field">
      <label>Tipo do produto</label>
      <input type="text" name="product[type]" value="<%= product.type %>">
    </div>


    <button class="ui button">Enviar</button>
  </form>
</form>


</div>

<% include ../partials/footer %>
